import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
    carName: {
        fontSize: 25,
        fontWeight: 'bolder',
        marginBottom: 15,
    },
    carPaper: {
        marginTop: 40,
        paddingTop: 50,
        paddingBottom: 50,
    },
})

export default useStyles
